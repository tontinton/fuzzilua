diff --git a/deps/lua/src/lgc.c b/deps/lua/src/lgc.c
index e909c79a9696..76797d43f2be 100644
--- a/deps/lua/src/lgc.c
+++ b/deps/lua/src/lgc.c
@@ -6,6 +6,11 @@
 
 #include <string.h>
 
+#ifdef FUZZILUA_GC_ASSERT
+#include <stdio.h>
+#include <stdlib.h>
+#endif
+
 #define lgc_c
 #define LUA_CORE
 
@@ -23,6 +28,135 @@
 #include "ltm.h"
 
 
+#ifdef FUZZILUA_GC_ASSERT
+
+#define fuzzilua_assert(cond, msg) do { \
+  if (!(cond)) { \
+    fprintf(stderr, "FUZZILUA GC ASSERT: %s at %s:%d\n", \
+            (msg), __FILE__, __LINE__); \
+    abort(); \
+  } \
+} while(0)
+
+/* Minimum valid GC object type is LUA_TSTRING (4) and max is LUA_TUPVAL (10) */
+#define FUZZILUA_VALID_GC_TT(tt) \
+  ((tt) >= LUA_TSTRING && (tt) <= LUA_TUPVAL)
+
+/*
+** Check that a TValue does not reference a white object from a black parent.
+** Only meaningful during GCSpropagate when the tri-color invariant must hold.
+*/
+static void fuzzilua_check_tvalue_no_black_white(global_State *g,
+                                                  const TValue *slot) {
+  if (!iscollectable(slot)) return;
+  GCObject *child = gcvalue(slot);
+  fuzzilua_assert(FUZZILUA_VALID_GC_TT(child->gch.tt),
+                  "TValue references GCObject with corrupt type tag");
+  if (iswhite(child)) {
+    /* A white child reachable from a black parent violates the invariant */
+    fuzzilua_assert(0,
+      "black->white edge detected (tri-color invariant violation)");
+  }
+}
+
+/*
+** Walk the children of a black object and verify no white references exist.
+** This is only called during GCSpropagate.
+*/
+static void fuzzilua_check_no_black_white(global_State *g, GCObject *o) {
+  int i;
+  if (g->gcstate != GCSpropagate) return;
+  if (!isblack(o)) return;
+  switch (o->gch.tt) {
+    case LUA_TTABLE: {
+      Table *h = gco2h(o);
+      /* Skip weak tables -- they may intentionally hold white refs */
+      if (testbit(h->marked, KEYWEAKBIT) || testbit(h->marked, VALUEWEAKBIT))
+        return;
+      if (h->metatable) {
+        fuzzilua_assert(!iswhite(obj2gco(h->metatable)),
+                        "black table -> white metatable");
+      }
+      for (i = 0; i < h->sizearray; i++)
+        fuzzilua_check_tvalue_no_black_white(g, &h->array[i]);
+      for (i = 0; i < sizenode(h); i++) {
+        Node *n = gnode(h, i);
+        if (!ttisnil(gval(n))) {
+          fuzzilua_check_tvalue_no_black_white(g, gval(n));
+          fuzzilua_check_tvalue_no_black_white(g, key2tval(n));
+        }
+      }
+      break;
+    }
+    case LUA_TFUNCTION: {
+      Closure *cl = gco2cl(o);
+      fuzzilua_assert(!iswhite(obj2gco(cl->c.env)),
+                      "black closure -> white env");
+      if (cl->c.isC) {
+        for (i = 0; i < cl->c.nupvalues; i++)
+          fuzzilua_check_tvalue_no_black_white(g, &cl->c.upvalue[i]);
+      } else {
+        fuzzilua_assert(!iswhite(obj2gco(cl->l.p)),
+                        "black Lua closure -> white proto");
+        for (i = 0; i < cl->l.nupvalues; i++) {
+          fuzzilua_assert(!iswhite(obj2gco(cl->l.upvals[i])),
+                          "black Lua closure -> white upval");
+        }
+      }
+      break;
+    }
+    case LUA_TUSERDATA: {
+      if (gco2u(o)->metatable) {
+        fuzzilua_assert(!iswhite(obj2gco(gco2u(o)->metatable)),
+                        "black udata -> white metatable");
+      }
+      fuzzilua_assert(!iswhite(obj2gco(gco2u(o)->env)),
+                      "black udata -> white env");
+      break;
+    }
+    case LUA_TPROTO: {
+      Proto *p = gco2p(o);
+      for (i = 0; i < p->sizek; i++)
+        fuzzilua_check_tvalue_no_black_white(g, &p->k[i]);
+      for (i = 0; i < p->sizep; i++) {
+        if (p->p[i]) {
+          fuzzilua_assert(!iswhite(obj2gco(p->p[i])),
+                          "black proto -> white nested proto");
+        }
+      }
+      break;
+    }
+    /* LUA_TTHREAD: threads are always grayagain, never stay black */
+    /* LUA_TUPVAL: closed upvals that are black have their value checked */
+    default:
+      break;
+  }
+}
+
+/*
+** Full GC invariant check: walk rootgc list and verify all objects.
+** Called periodically from the allocation stress hook.
+*/
+void fuzzilua_gc_check_invariants(global_State *g) {
+  GCObject *o;
+  int count = 0;
+  /* Only check during propagate phase when the invariant must hold */
+  if (g->gcstate != GCSpropagate) return;
+  for (o = g->rootgc; o != NULL; o = o->gch.next) {
+    /* Every object must have a valid type tag */
+    fuzzilua_assert(FUZZILUA_VALID_GC_TT(o->gch.tt),
+                    "rootgc list contains object with corrupt type tag");
+    /* No black object should point to a white object */
+    if (isblack(o))
+      fuzzilua_check_no_black_white(g, o);
+    /* Safety bound: don't spend too long in the check */
+    if (++count > 10000) break;
+  }
+}
+
+#endif /* FUZZILUA_GC_ASSERT */
+
+
 #define GCSTEPSIZE	1024u
 #define GCSWEEPMAX	40
 #define GCSWEEPCOST	10
@@ -68,6 +202,14 @@ static void removeentry (Node *n) {
 
 static void reallymarkobject (global_State *g, GCObject *o) {
   lua_assert(iswhite(o) && !isdead(g, o));
+#ifdef FUZZILUA_GC_ASSERT
+  fuzzilua_assert(iswhite(o),
+    "reallymarkobject: object being marked is not white");
+  fuzzilua_assert(!isdead(g, o),
+    "reallymarkobject: marking a dead (wrong-generation) object");
+  fuzzilua_assert(FUZZILUA_VALID_GC_TT(o->gch.tt),
+    "reallymarkobject: object has invalid type tag");
+#endif
   white2gray(o);
   switch (o->gch.tt) {
     case LUA_TSTRING: {
@@ -277,6 +419,12 @@ static void traversestack (global_State *g, lua_State *l) {
 static l_mem propagatemark (global_State *g) {
   GCObject *o = g->gray;
   lua_assert(isgray(o));
+#ifdef FUZZILUA_GC_ASSERT
+  fuzzilua_assert(isgray(o),
+    "propagatemark: object on gray list is not gray");
+  fuzzilua_assert(FUZZILUA_VALID_GC_TT(o->gch.tt),
+    "propagatemark: gray object has invalid type tag");
+#endif
   gray2black(o);
   switch (o->gch.tt) {
     case LUA_TTABLE: {
@@ -284,6 +432,13 @@ static l_mem propagatemark (global_State *g) {
       g->gray = h->gclist;
       if (traversetable(g, h))  /* table is weak? */
         black2gray(o);  /* keep it gray */
+#ifdef FUZZILUA_GC_ASSERT
+      /* After traversal: must be black (strong table) or gray (weak table) */
+      if (!testbit(h->marked, KEYWEAKBIT) && !testbit(h->marked, VALUEWEAKBIT))
+        fuzzilua_assert(isblack(o),
+          "propagatemark: strong table not black after propagation");
+      fuzzilua_check_no_black_white(g, o);
+#endif
       return sizeof(Table) + sizeof(TValue) * h->sizearray +
                              sizeof(Node) * sizenode(h);
     }
@@ -291,6 +446,11 @@ static l_mem propagatemark (global_State *g) {
       Closure *cl = gco2cl(o);
       g->gray = cl->c.gclist;
       traverseclosure(g, cl);
+#ifdef FUZZILUA_GC_ASSERT
+      fuzzilua_assert(isblack(o),
+        "propagatemark: closure not black after propagation");
+      fuzzilua_check_no_black_white(g, o);
+#endif
       return (cl->c.isC) ? sizeCclosure(cl->c.nupvalues) :
                            sizeLclosure(cl->l.nupvalues);
     }
@@ -301,6 +461,11 @@ static l_mem propagatemark (global_State *g) {
       g->grayagain = o;
       black2gray(o);
       traversestack(g, th);
+#ifdef FUZZILUA_GC_ASSERT
+      /* Threads are always put back to gray (grayagain), so should be gray */
+      fuzzilua_assert(isgray(o),
+        "propagatemark: thread not gray after propagation");
+#endif
       return sizeof(lua_State) + sizeof(TValue) * th->stacksize +
                                  sizeof(CallInfo) * th->size_ci;
     }
@@ -308,9 +473,14 @@ static l_mem propagatemark (global_State *g) {
       Proto *p = gco2p(o);
       g->gray = p->gclist;
       traverseproto(g, p);
+#ifdef FUZZILUA_GC_ASSERT
+      fuzzilua_assert(isblack(o),
+        "propagatemark: proto not black after propagation");
+      fuzzilua_check_no_black_white(g, o);
+#endif
       return sizeof(Proto) + sizeof(Instruction) * p->sizecode +
                              sizeof(Proto *) * p->sizep +
-                             sizeof(TValue) * p->sizek + 
+                             sizeof(TValue) * p->sizek +
                              sizeof(int) * p->sizelineinfo +
                              sizeof(LocVar) * p->sizelocvars +
                              sizeof(TString *) * p->sizeupvalues;
@@ -409,6 +579,10 @@ static GCObject **sweeplist (lua_State *L, GCObject **p, lu_mem count) {
   global_State *g = G(L);
   int deadmask = otherwhite(g);
   while ((curr = *p) != NULL && count-- > 0) {
+#ifdef FUZZILUA_GC_ASSERT
+    fuzzilua_assert(FUZZILUA_VALID_GC_TT(curr->gch.tt),
+      "sweeplist: object in GC list has corrupt type tag");
+#endif
     if (curr->gch.tt == LUA_TTHREAD)  /* sweep open upvalues of each thread */
       sweepwholelist(L, &gco2th(curr)->openupval);
     if ((curr->gch.marked ^ WHITEBITS) & deadmask) {  /* not dead? */
@@ -418,6 +592,10 @@ static GCObject **sweeplist (lua_State *L, GCObject **p, lu_mem count) {
     }
     else {  /* must erase `curr' */
       lua_assert(isdead(g, curr) || deadmask == bitmask(SFIXEDBIT));
+#ifdef FUZZILUA_GC_ASSERT
+      fuzzilua_assert(isdead(g, curr) || deadmask == bitmask(SFIXEDBIT),
+        "sweeplist: freeing object that is not dead");
+#endif
       *p = curr->gch.next;
       if (curr == g->rootgc)  /* is the first element of the list? */
         g->rootgc = curr->gch.next;  /* adjust first */
@@ -610,6 +788,19 @@ static l_mem singlestep (lua_State *L) {
 void luaC_step (lua_State *L) {
   global_State *g = G(L);
   l_mem lim = (GCSTEPSIZE/100) * g->gcstepmul;
+#ifdef FUZZILUA_GC_ASSERT
+  /* Periodic full-walk invariant check at a safe point. Unlike
+   * fuzzilua_gc_stress_hook (which fires inside luaM_realloc_ while
+   * caller pointers may still be stale), we are outside any allocation
+   * here, so every Proto->k / closure upval / etc. is consistent. The
+   * check early-exits unless gcstate == GCSpropagate. */
+  { static unsigned int fuzzilua_step_check_counter = 0;
+    if (++fuzzilua_step_check_counter >= 64) {
+      fuzzilua_step_check_counter = 0;
+      fuzzilua_gc_check_invariants(g);
+    }
+  }
+#endif
   if (lim == 0)
     lim = (MAX_LUMEM-1)/2;  /* no limit */
   g->gcdept += g->totalbytes - g->GCthreshold;
@@ -663,11 +854,32 @@ void luaC_barrierf (lua_State *L, GCObject *o, GCObject *v) {
   lua_assert(isblack(o) && iswhite(v) && !isdead(g, v) && !isdead(g, o));
   lua_assert(g->gcstate != GCSfinalize && g->gcstate != GCSpause);
   lua_assert(ttype(&o->gch) != LUA_TTABLE);
+#ifdef FUZZILUA_GC_ASSERT
+  fuzzilua_assert(isblack(o),
+    "barrierf: parent object is not black");
+  fuzzilua_assert(iswhite(v),
+    "barrierf: child object is not white");
+  fuzzilua_assert(!isdead(g, v),
+    "barrierf: child object is dead");
+  fuzzilua_assert(!isdead(g, o),
+    "barrierf: parent object is dead");
+  fuzzilua_assert(FUZZILUA_VALID_GC_TT(o->gch.tt),
+    "barrierf: parent has invalid type tag");
+  fuzzilua_assert(FUZZILUA_VALID_GC_TT(v->gch.tt),
+    "barrierf: child has invalid type tag");
+#endif
   /* must keep invariant? */
   if (g->gcstate == GCSpropagate)
     reallymarkobject(g, v);  /* restore invariant */
   else  /* don't mind */
     makewhite(g, o);  /* mark as white just to avoid other barriers */
+#ifdef FUZZILUA_GC_ASSERT
+  /* After barrier: if propagating, child must no longer be white */
+  if (g->gcstate == GCSpropagate) {
+    fuzzilua_assert(!iswhite(v),
+      "barrierf: child still white after forward barrier in propagate phase");
+  }
+#endif
 }
 
 
@@ -676,9 +888,21 @@ void luaC_barrierback (lua_State *L, Table *t) {
   GCObject *o = obj2gco(t);
   lua_assert(isblack(o) && !isdead(g, o));
   lua_assert(g->gcstate != GCSfinalize && g->gcstate != GCSpause);
+#ifdef FUZZILUA_GC_ASSERT
+  fuzzilua_assert(isblack(o),
+    "barrierback: table is not black");
+  fuzzilua_assert(!isdead(g, o),
+    "barrierback: table is dead");
+  fuzzilua_assert(o->gch.tt == LUA_TTABLE,
+    "barrierback: object is not a table");
+#endif
   black2gray(o);  /* make table gray (again) */
   t->gclist = g->grayagain;
   g->grayagain = o;
+#ifdef FUZZILUA_GC_ASSERT
+  fuzzilua_assert(isgray(o),
+    "barrierback: table not gray after back barrier");
+#endif
 }
 
 
diff --git a/deps/lua/src/lgc.h b/deps/lua/src/lgc.h
index 5a8dc605b319..e6ea569a3109 100644
--- a/deps/lua/src/lgc.h
+++ b/deps/lua/src/lgc.h
@@ -106,5 +106,10 @@ LUAI_FUNC void luaC_linkupval (lua_State *L, UpVal *uv);
 LUAI_FUNC void luaC_barrierf (lua_State *L, GCObject *o, GCObject *v);
 LUAI_FUNC void luaC_barrierback (lua_State *L, Table *t);
 
+#ifdef FUZZILUA_GC_ASSERT
+struct global_State;
+void fuzzilua_gc_check_invariants(struct global_State *g);
+#endif
+
 
 #endif
diff --git a/deps/lua/src/lmem.c b/deps/lua/src/lmem.c
index ae7d8c965f6e..2c3c8ad32e56 100644
--- a/deps/lua/src/lmem.c
+++ b/deps/lua/src/lmem.c
@@ -5,6 +5,19 @@
 */
 
 
+#ifdef FUZZILUA_GC_STRESS
+#include <sys/mman.h>
+#include <sys/stat.h>
+#include <fcntl.h>
+#include <stdlib.h>
+#include <unistd.h>
+#include <stdint.h>
+#endif
+
+#ifdef FUZZILUA_ALLOC_FAIL
+#include <stdlib.h>
+#endif
+
 #include <stddef.h>
 
 #define lmem_c
@@ -18,6 +31,86 @@
 #include "lobject.h"
 #include "lstate.h"
 
+#ifdef FUZZILUA_GC_STRESS
+#include "lgc.h"
+
+#ifndef FUZZILUA_GC_STRESS_INTERVAL
+#define FUZZILUA_GC_STRESS_INTERVAL 1
+#endif
+
+#ifndef FUZZILUA_GC_STRESS_MODE
+#define FUZZILUA_GC_STRESS_MODE 1
+#endif
+
+#define FUZZILUA_GC_BITMAP_SIZE FUZZILUA_BITMAP_SIZE
+
+static uint8_t *fuzzilua_gc_bitmap = NULL;
+static uint32_t fuzzilua_gc_bitmap_size = 0;
+static uint32_t fuzzilua_alloc_counter = 0;
+
+__attribute__((constructor))
+static void fuzzilua_gc_init(void) {
+    const char *shm_name = getenv("FUZZILUA_SHM_GC");
+    if (!shm_name || shm_name[0] == '\0')
+        return;
+
+    int fd = shm_open(shm_name, O_RDWR, 0);
+    if (fd < 0)
+        return;
+
+    /* Layout: [edge (FUZZILUA_BITMAP_SIZE) | gc (FUZZILUA_BITMAP_SIZE)] */
+    size_t total_size = FUZZILUA_GC_BITMAP_SIZE * 2;
+    uint8_t *base = (uint8_t *)mmap(NULL, total_size, PROT_READ | PROT_WRITE,
+                                     MAP_SHARED, fd, 0);
+    close(fd);
+
+    if (base == MAP_FAILED)
+        return;
+
+    fuzzilua_gc_bitmap = base + FUZZILUA_GC_BITMAP_SIZE;
+    fuzzilua_gc_bitmap_size = FUZZILUA_GC_BITMAP_SIZE;
+}
+
+static inline uint32_t fuzzilua_gc_hash(uintptr_t alloc_site,
+                                         uint8_t gc_phase) {
+    uint32_t h = (uint32_t)(alloc_site >> 4);
+    h ^= (uint32_t)gc_phase * 0x9e3779b1u;
+    return h;
+}
+
+static inline void fuzzilua_record_gc_coverage(lua_State *L,
+                                                uintptr_t alloc_site) {
+    if (!fuzzilua_gc_bitmap || !G(L)->GCthreshold)
+        return;
+
+    global_State *g = G(L);
+    uint8_t phase = (uint8_t)g->gcstate;
+    uint32_t idx = fuzzilua_gc_hash(alloc_site, phase)
+                   % fuzzilua_gc_bitmap_size;
+
+    fuzzilua_gc_bitmap[idx] = 1;
+}
+
+static inline void fuzzilua_gc_stress_hook(lua_State *L) {
+    global_State *g = G(L);
+    if (!g->GCthreshold)
+        return;
+    if (++fuzzilua_alloc_counter >= FUZZILUA_GC_STRESS_INTERVAL) {
+        fuzzilua_alloc_counter = 0;
+        g->GCthreshold = g->totalbytes;
+        /* NOTE: do NOT run fuzzilua_gc_check_invariants() from here.
+         * This hook fires inside luaM_realloc_, before callers have had a
+         * chance to write the returned (possibly relocated) pointer back
+         * into their object graph. A GC walk at this point would observe
+         * stale pointers (e.g. Proto->k still pointing at the freed old
+         * buffer during luaM_reallocvector), producing false-positive
+         * use-after-frees. The full-walk check runs from luaC_step
+         * instead, where caller state is guaranteed consistent. */
+    }
+}
+
+#endif /* FUZZILUA_GC_STRESS */
+
 
 
 /*
@@ -40,6 +133,44 @@
 
 
 
+#ifdef FUZZILUA_ALLOC_FAIL
+
+static double fuzzilua_alloc_fail_prob = 0.0;
+static unsigned int fuzzilua_alloc_fail_seed = 0;
+static int fuzzilua_alloc_fail_active = 0;
+static unsigned int fuzzilua_alloc_warmup = 0;
+
+#ifndef FUZZILUA_ALLOC_WARMUP_COUNT
+#define FUZZILUA_ALLOC_WARMUP_COUNT 50000
+#endif
+
+static void fuzzilua_alloc_fail_init(void) {
+    if (fuzzilua_alloc_fail_active)
+        return;
+
+    if (++fuzzilua_alloc_warmup < FUZZILUA_ALLOC_WARMUP_COUNT)
+        return;
+
+    const char *env = getenv("FUZZILUA_ALLOC_FAIL_PROB");
+    if (env && env[0] != '\0')
+        fuzzilua_alloc_fail_prob = strtod(env, NULL);
+
+    fuzzilua_alloc_fail_seed = (unsigned int)getpid() ^ 0xdeadbeef;
+    fuzzilua_alloc_fail_active = 1;
+}
+
+static int fuzzilua_should_fail(void) {
+    if (fuzzilua_alloc_fail_prob <= 0.0)
+        return 0;
+    fuzzilua_alloc_fail_seed ^= fuzzilua_alloc_fail_seed << 13;
+    fuzzilua_alloc_fail_seed ^= fuzzilua_alloc_fail_seed >> 17;
+    fuzzilua_alloc_fail_seed ^= fuzzilua_alloc_fail_seed << 5;
+    double r = (double)fuzzilua_alloc_fail_seed / (double)0xFFFFFFFFu;
+    return r < fuzzilua_alloc_fail_prob;
+}
+
+#endif /* FUZZILUA_ALLOC_FAIL */
+
 #define MINSIZEARRAY	4
 
 
@@ -76,11 +207,31 @@ void *luaM_toobig (lua_State *L) {
 void *luaM_realloc_ (lua_State *L, void *block, size_t osize, size_t nsize) {
   global_State *g = G(L);
   lua_assert((osize == 0) == (block == NULL));
+#ifdef FUZZILUA_ALLOC_FAIL
+  /* Inject allocation failures BEFORE calling the real allocator.
+   * This is critical for realloc: if the real realloc succeeds, it frees
+   * the old block. Pretending it failed after that creates a dangling
+   * pointer (caller still holds old ptr) -- an impossible state that a
+   * real allocator can never produce. By injecting before, the old block
+   * is preserved, matching real OOM semantics. */
+  if (nsize > 0 && nsize > osize) {
+    fuzzilua_alloc_fail_init();
+    if (fuzzilua_should_fail()) {
+      luaD_throw(L, LUA_ERRMEM);
+    }
+  }
+#endif
   block = (*g->frealloc)(g->ud, block, osize, nsize);
   if (block == NULL && nsize > 0)
     luaD_throw(L, LUA_ERRMEM);
   lua_assert((nsize == 0) == (block == NULL));
   g->totalbytes = (g->totalbytes - osize) + nsize;
+
+#ifdef FUZZILUA_GC_STRESS
+  if (nsize > 0) {
+    fuzzilua_record_gc_coverage(L, (uintptr_t)__builtin_return_address(0));
+    fuzzilua_gc_stress_hook(L);
+  }
+#endif
   return block;
 }
-
diff --git a/src/Makefile b/src/Makefile
index 24b9a39ba2a5..c61f67eabb88 100644
--- a/src/Makefile
+++ b/src/Makefile
@@ -382,7 +382,7 @@ endif
 
 REDIS_SERVER_NAME=redis-server$(PROG_SUFFIX)
 REDIS_SENTINEL_NAME=redis-sentinel$(PROG_SUFFIX)
-REDIS_SERVER_OBJ=threads_mngr.o memory_prefetch.o adlist.o quicklist.o ae.o anet.o dict.o ebuckets.o eventnotifier.o iothread.o mstr.o entry.o kvstore.o fwtree.o estore.o server.o sds.o zmalloc.o lzf_c.o lzf_d.o pqsort.o zipmap.o sha1.o ziplist.o release.o networking.o util.o object.o db.o replication.o rdb.o t_string.o t_list.o t_set.o t_zset.o t_hash.o config.o aof.o pubsub.o multi.o debug.o sort.o intset.o syncio.o cluster.o cluster_asm.o cluster_legacy.o cluster_slot_stats.o crc16.o endianconv.o slowlog.o eval.o bio.o rio.o rand.o memtest.o syscheck.o crcspeed.o crccombine.o crc64.o bitops.o sentinel.o notify.o setproctitle.o blocked.o hyperloglog.o latency.o sparkline.o redis-check-rdb.o redis-check-aof.o geo.o lazyfree.o module.o evict.o expire.o geohash.o geohash_helper.o childinfo.o defrag.o siphash.o rax.o t_stream.o listpack.o localtime.o lolwut.o lolwut5.o lolwut6.o lolwut8.o acl.o tracking.o socket.o tls.o sha256.o timeout.o setcpuaffinity.o monotonic.o mt19937-64.o resp_parser.o call_reply.o script_lua.o script.o functions.o function_lua.o commands.o strl.o connection.o unix.o logreqres.o keymeta.o chk.o hotkeys.o
+REDIS_SERVER_OBJ=fuzzilua_coverage.o threads_mngr.o memory_prefetch.o adlist.o quicklist.o ae.o anet.o dict.o ebuckets.o eventnotifier.o iothread.o mstr.o entry.o kvstore.o fwtree.o estore.o server.o sds.o zmalloc.o lzf_c.o lzf_d.o pqsort.o zipmap.o sha1.o ziplist.o release.o networking.o util.o object.o db.o replication.o rdb.o t_string.o t_list.o t_set.o t_zset.o t_hash.o config.o aof.o pubsub.o multi.o debug.o sort.o intset.o syncio.o cluster.o cluster_asm.o cluster_legacy.o cluster_slot_stats.o crc16.o endianconv.o slowlog.o eval.o bio.o rio.o rand.o memtest.o syscheck.o crcspeed.o crccombine.o crc64.o bitops.o sentinel.o notify.o setproctitle.o blocked.o hyperloglog.o latency.o sparkline.o redis-check-rdb.o redis-check-aof.o geo.o lazyfree.o module.o evict.o expire.o geohash.o geohash_helper.o childinfo.o defrag.o siphash.o rax.o t_stream.o listpack.o localtime.o lolwut.o lolwut5.o lolwut6.o lolwut8.o acl.o tracking.o socket.o tls.o sha256.o timeout.o setcpuaffinity.o monotonic.o mt19937-64.o resp_parser.o call_reply.o script_lua.o script.o functions.o function_lua.o commands.o strl.o connection.o unix.o logreqres.o keymeta.o chk.o hotkeys.o
 REDIS_CLI_NAME=redis-cli$(PROG_SUFFIX)
 REDIS_CLI_OBJ=anet.o adlist.o dict.o redis-cli.o zmalloc.o release.o ae.o redisassert.o crcspeed.o crccombine.o crc64.o siphash.o crc16.o monotonic.o cli_common.o mt19937-64.o strl.o cli_commands.o
 REDIS_BENCHMARK_NAME=redis-benchmark$(PROG_SUFFIX)
